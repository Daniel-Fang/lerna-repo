{
  "name": "mod1",
  "version": "1.0.0",
  "description": "Module1 of monorepo",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && babel --root-mode upward src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Alex",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.13.16",
    "@babel/preset-env": "^7.14.1"
  }
}
