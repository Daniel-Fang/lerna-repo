{
  "name": "lerna-repo",
  "version": "1.0.0",
  "description": "A monorepo for test Babel",
  "scripts": {
    "build": "rm -rf dist && babel packages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Alex",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.13.16",
    "@babel/core": "^7.14.0",
    "@babel/plugin-transform-runtime": "^7.13.15",
    "@babel/preset-env": "^7.14.1",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "core-js": "^3.12.1"
  }
}
